{
  "name": "opengiveaway",
  "version": "0.1.0",
  "description": "Self-hostable, cryptographically verifiable giveaway system. Commit the giveaway state, anchor it to Bitcoin via OpenTimestamps, then draw winners from predetermined Bitcoin block randomness.",
  "type": "module",
  "license": "MIT",
  "bin": {
    "opengiveaway": "./dist/cli/index.js",
    "opengiveaway-verify": "./dist/verify/cli.js"
  },
  "exports": {
    ".": "./dist/protocol/index.js",
    "./verify": "./dist/verify/core.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json && npm run build:web",
    "build:web": "node scripts/build-web.mjs",
    "cli": "tsx src/cli/index.ts",
    "verify": "tsx src/verify/cli.ts",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.115.0",
    "commander": "^12.1.0",
    "csv-parse": "^5.5.6"
  },
  "optionalDependencies": {
    "opentimestamps": "^0.4.9"
  },
  "devDependencies": {
    "@types/node": "^22.7.4",
    "esbuild": "^0.28.2",
    "tsx": "^4.19.1",
    "typescript": "^5.6.2",
    "vitest": "^2.1.1"
  },
  "engines": {
    "node": ">=20"
  }
}
