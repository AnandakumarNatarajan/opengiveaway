// The `opentimestamps` package ships no type declarations and is an optional
// dependency; declare it as `any` so the wrapper in ots.ts type-checks whether
// or not the package is installed.
declare module "opentimestamps";
